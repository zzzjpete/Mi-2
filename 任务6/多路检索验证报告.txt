================================================================================================
多路检索 + 多准则重排 · 端到端验证
================================================================================================
[流水线] 初始化查询理解处理器 ...
[词典] MeSH 26,694 主题词 / 107,944 词面
[流水线] 打开 Chroma 集合 medrag_bge_base @ E:\rag\data\chroma_db_4m ...
[流水线] 加载嵌入模型 bge-base ...
[模型] 加载 BAAI/bge-base-en-v1.5 (dim=768) 到 cuda ...
[流水线] mmap 载入 BM25 索引 @ E:\rag\data\bm25_index_4m ...
[重排] 加载 BAAI/bge-reranker-base 到 cuda ...
[流水线] 就绪：Chroma 3,998,000 向量 | BM25 3,998,000 篇

[加载完成] 用时 30s

================================================================================================
A. 多路检索 —— 向量/关键词各自返回，BM25 提供独立召回
================================================================================================
  [PASS] 向量检索返回非空 — 3 个变体，共 150 条命中
  [PASS] BM25 关键词检索返回非空 — BM25 top50 命中 50 条
  [PASS] BM25 贡献了向量 top-k 之外的候选（独立召回） — 关键词独有 48 / 50 条，向量∩关键词 2 条

================================================================================================
B. 融合策略 —— simple / rrf / weighted 打分公式独立重算 + 排序单调
================================================================================================
  [PASS] RRF 分数 = Σ w/(k+rank)（逐候选独立重算一致） — 最大误差 0.00e+00，候选 125
  [PASS] RRF 排序按分数单调递减
  [PASS] weighted 分数 = vw·norm(cos)+(1-vw)·norm(bm25)（独立重算一致） — vw=0.7，最大误差 0.00e+00
  [PASS] simple 去重合并：命中两路者全部排在只命中一路者之前 — 两路命中 2 条，单路 123 条
  [PASS] 三种策略作用于同一候选并集（策略只改定序不改集合） — 并集大小 125

================================================================================================
C. 过滤对关键词来源候选生效 —— 构造成必有 BM25 候选被过滤才算通过
================================================================================================
    [融合] 过滤丢弃 449 条（不满足 filters/后置过滤或未在库中）
    [多路检索] 向量 3变体×500(共1500) 0.025s | BM25 top500 0.046s | 融合(rrf,后过滤×10) → 724 候选 / 返回 724  总 0.617s
  [PASS] 过滤 pub_year>=2021 下 BM25 命中里 <阈值 的候选被剔除（非空洞） — BM25 命中 <2021 有 23 条，最终泄漏 0 条
  [PASS] 过滤后全部最终候选 pub_year>=阈值（含关键词来源） — 最终 724 条，最低年份 2021
  [PASS] methods 章节走后置过滤（section_canon=methods，未下推 $in） — filters={} post={"section_canon": "methods"}
    [融合] 过滤丢弃 519 条（不满足 filters/后置过滤或未在库中）
    [多路检索] 向量 1变体×500(共500) 0.040s | BM25 top500 0.036s | 融合(rrf,后过滤×10) → 466 候选 / 返回 466  总 0.509s
  [PASS] section 后置过滤把非-methods 的 BM25 候选剔除（非空洞） — BM25 命中里非-methods 45 条，最终泄漏 0 条
  [PASS] 过滤后全部最终候选 section∈methods 写法表（复用同一 canonical_to_raw） — 最终 466 条

================================================================================================
D. 多准则重排 —— relevance × recency × authority
================================================================================================
  [PASS] 准则权重之和为 1 — 权重 {'relevance': 0.6, 'recency': 0.25, 'authority': 0.15}
  [PASS] 时效性随年份单调递减且∈[0,1] — 今年=1.00 前10年=0.50 前100年=0.00
  [PASS] 权威性 已知顶刊>开放获取巨刊>未知刊 且∈[0,1] — Nature=1.00 PLoS ONE=0.62 未知=0.50
    [融合] 过滤丢弃 533 条（不满足 filters/后置过滤或未在库中）
    [多路检索] 向量 1变体×500(共500) 0.014s | BM25 top500 0.050s | 融合(rrf,后过滤×10) → 431 候选 / 返回 50  总 0.460s
    [重排] 50 候选过交叉编码器（mode=tiebreak, ε=0.02），用时 0.54s
  [PASS] 重排返回非空 — 10 条
  [PASS] 三准则分数均∈[0,1]
  [PASS] 重排总分 = Σ 准则权重×准则分（逐条独立重算一致） — 最大误差 0.00e+00
  [PASS] 最终结果按 sort_key 单调递减（mode=tiebreak） — 排序键 5 级
  [PASS] ① rel 分档单调不增（相关性仍是第一位） — 分档 [35, 34, 32, 31, 27, 24, 21, 20, 19, 17]
  [PASS] ② 同档内 recency 单调不增（同等相关时新的优先，全池非空洞检验） — 全池 50 条，同档相邻对 27 处
  [PASS] ③ 排序键在全池上构成全序（无残余并列 → 名次不依赖上游融合顺序） — 50 条候选，唯一键 50 个
  [PASS] ④ weighted 模式仍按总分单调递减（旧行为可随时切回做对照） — 全池 50 条
  [PASS] 重排相对纯融合序发生了重排（顺序被改变） — top10 顺序 不同

================================================================================================
验证结论：25/25 项通过  →  全部通过
================================================================================================
[总用时 33s]
