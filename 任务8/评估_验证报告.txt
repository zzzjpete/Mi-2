============================================================================================
阶段八验证 · 答案评估器 / 缓存 / 批量处理
时间：2026-08-16 19:27:07   ROUGE 后端：rouge-lib   CPU 逻辑核：24
每条 PASS/FAIL 均由实际算出的变量比对得出，无一条是无条件打印。

============================================================================================
A 文本相似性 · ROUGE（后端：rouge-lib）
============================================================================================
  [PASS] 完全相同的文本 ROUGE-L f ≈ 1   f=0.999999995（rouge 库分母含 1e-8，故非严格 1.0）
  [PASS] 毫无重合的文本各项 ROUGE 全为 0   1/2/L f = [0.0, 0.0, 0.0]
  [PASS] 部分重合的文本落在 (0,1) 之间   ROUGE-L f=0.6667
  [PASS] ROUGE-1 ≥ ROUGE-2（n 越大越难重合）   0.6667 ≥ 0.2105
  [PASS] 中文按空白分词会塌成 1 个 token（所以不能直接喂 rouge 库）   空白分词 1 个 token / 混合分词 22 个
  [PASS] 不预分词时两句高度相似的中文被判为 0；预分词后能分辨   未预分词 f=0.0000 → 预分词后 f=0.7907
  [PASS] 小数点被保护，「.」只当句子分隔符（否则 0.45 会被 rouge 库当句号劈开）   直接拼接含 2 个句点（都是小数点）→ 处理后 1 个（真句子边界）
  [PASS] 超长文本：库直接喂会 RecursionError，本模块按句切分后不崩   库对 3000 词单句栈溢出=True；本模块对 2169 字文本正常出分
  [PASS] 空文本不抛异常且记 0（rouge 库本身会抛 ValueError）   两种空输入均返回 0
  [PASS] 自研实现与 rouge 库在 4 份真实答案上数值一致   27 个数值逐个比对，最大差值 0.00e+00

============================================================================================
B 关键信息抽取与召回
============================================================================================
  [PASS] 百分比：符号式与中文式都能抽   ['12.6%', '百分之30']
  [PASS] 剂量：数值+单位，含 /kg 与无空格写法   ['10mg/kg', '500mg', '800iu']
  [PASS] 时间范围：中文量词「个」、中文数字「两」、区间都能抽   ['18month', '2week', '4-6week']
  [PASS] 安全信息：中英同义写法归到同一 concept（否则跨语种召回恒为 0）   中文 ['adverse_effect', 'risk'] ∩ 英文 ['adverse_effect', 'risk', 'safety'] = ['adverse_effect', 'risk']
  [PASS] 召回率 = overlap / gt_matches（与手算一致）   overlap=2 gt=3 recall=0.6667
  [PASS] 漏掉的信息被列出来（可定位是哪条丢了）   missed=['55%']
  [PASS] 参照为空时 recall 记 None 而不是 0（没参照 ≠ 零召回）   recall=None
  [PASS] 归一化：「10 mg/kg」与「10mg/kg」视为同一条信息   recall=1.0
  [PASS] 答案与参照完全相同时总召回 = 1.0   4/4
  [PASS] 任务书点名的六类字段齐全   ['percentage', 'dosage', 'duration', 'safety', 'recommendation', 'mechanism']

============================================================================================
C 幻觉信号检测
============================================================================================
  [PASS] 措辞谨慎且带出处的答案：无未缓解信号，风险 0   信号 0 个，风险 0.0
  [PASS] 任务书点名的四类信号 + 全称量化，逐条都能命中   ['vague_citation', 'unqualified_proof', 'absolute_percentage', 'over_absolute', 'universal_quantifier'] → 命中 5/5
  [PASS] 同一句话，带 [S#] 记为 mitigated、不带才计入风险   带出处未缓解 0 / 不带 1
  [PASS] 严格模式下带出处的断言也计入风险（开关生效）   count_cited_as_signal=True
  [PASS] 信号越多风险分越高（同等篇幅，严格单调）   1 个 0.9481 < 2 个 0.9992 < 4 个 1.0000
  [PASS] 真实答案量级下不顶死在 1.0，仍能分出轻重   2400 字文本：1 个信号 0.1874 < 4 个信号 0.8064 < 1.0
        （信号密度极高的短文本仍会逼近 1.0——那是渐近上界，不是截断。如上面 200 字含 4 个信号的例子实测 1.0000）
  [PASS] 风险分随篇幅归一（长答案不会仅因为字多就被判高危）   同样 1 个信号，篇幅 ×4 后风险 0.5372 < 0.9481
  [PASS] 风险分恒在 [0,1] 区间   实测 [0.0, 0.948, 0.999, 1.0, 0.0, 1.0]
  [PASS] 句子切分不把 e.g. 的点当句号   切出 2 句：['给药方式 e.g. 静脉注射。', '随访 18 个月。']

============================================================================================
D 可读性
============================================================================================
  [PASS] 平均句子长度与手算一致   2 句，均 6.0 字（手算 6.0）
  [PASS] 长句比例按阈值算对   2 句中 1 句超过 80 字 → 0.5
  [PASS] 中英混排比例算得出（阶段七的已知缺陷，现在可量化）   4 句里 1 句异种语言 → 0.25
  [PASS] 能识别标题与列表结构   标题 1 个，列表 2 项
  [PASS] 空文本不崩，各项为 0   0 句
  [PASS] 可读性分恒在 [0,1] 区间   实测 [0.835, 0.638, 0.83, 0.772]

============================================================================================
E 缓存：键 / TTL / 容量 / 温度门限 / 落盘 / 线程安全
============================================================================================
  [PASS] 相同查询 + 相同上下文 → 同一个键（相同输入相同输出）   9491a26dbd75d7da1ab39430…
  [PASS] 换了上下文 → 键不同
  [PASS] 换了模型/温度参数 → 键不同
  [PASS] 键与参数书写顺序无关（canonical 序列化，不是 str(dict)）
  [PASS] 写入后能命中
  [PASS] 未写入的键返回 None
  [PASS] 命中时累加省下的耗时   seconds_saved=12.5
  [PASS] 温度 0.3 > 门限 0.0 → 拒绝写入（只缓存确定性输出）
  [PASS] 温度 0.0 ≤ 门限 → 允许写入
  [PASS] 容量上限触发 LRU 淘汰，淘汰的是最久未用的那条   淘汰 1 条，剩下 a=True b=False d=True
  [PASS] 字节上限生效，占用不超过 max_bytes   345 ≤ 400 字节，按字节淘汰 17 条
  [PASS] TTL 到期后读不到（医学知识有时效性，不能永久缓存）   过期前命中=True，过期后 miss=True
  [PASS] 落盘再载入：有效条目原样恢复，过期条目被丢弃   载入 1 条（写入 2 条，其中 1 条已过期）
  [PASS] 多线程并发读写：无异常、条目数与命中数都对得上   8 线程 × 400 次：条目 3200，命中 3200，异常 0 个

============================================================================================
F 批量处理：顺序 / 错误隔离 / 并发度
============================================================================================
  [PASS] 输出长度与输入一致   20 == 20
  [PASS] 输出顺序与输入一一对应（不是按完成先后）   逐下标核对 index 与 value
  [PASS] 单个任务失败不影响其他任务   失败 [3, 8, 13, 18]，成功 16 项
  [PASS] 失败项保留位置并带错误信息（不塌陷、不静默）   out[3].error = ValueError: 第 3 项故意失败
  [PASS] 并发度 1/3/8 得到完全相同的结果序列   顺序与取值均一致，说明并发度不影响语义
  [PASS] 并发度推荐值随工作类型区分且不超核数上限   CPU 逻辑核 24 → llm=4 cpu=23 io=32
  [PASS] 任务数少于并发度时按任务数收敛（不空开线程）
  [PASS] 空输入返回空列表且不报错   []
  [PASS] I/O 型任务并发确实更快（4 线程 vs 1 线程实测）   1 线程 1.204s → 4 线程 0.302s （3.99×）
        注：本项目的 LLM 批量瓶颈在单块 GPU，不在 CPU；真实加速比见 评估_跑测试集.py 的实测。

============================================================================================
G 缓存包装器与阶段七生成器的接口兼容（假生成器，不调模型）
============================================================================================
  [PASS] 未知属性/方法原样转发给被包装的生成器
  [PASS] 注入的缓存对象确实被用上（空缓存不会因 __len__ 为 0 而被 `or` 换掉）   len(cache)=0 但 bool(cache)=True，注入对象同一性=True
  [PASS] 相同调用第二次命中缓存，底层生成器只被调用一次   底层调用 1 次，第二次 cache_hit=True
  [PASS] 命中时不谎报耗时（elapsed 归零，原耗时记进 cache_saved_seconds）   elapsed=0.0 saved=1.0
  [PASS] 改了 system 提示词就不再命中（提示词进了键，杜绝读到旧答案）   底层调用 2 次
  [PASS] 温度 0.3 的调用不走缓存（每次都真调模型）   底层调用从 2 增到 4
  [PASS] 失败结果不入缓存（否则把一次故障钉死成永久答案）   两次调用都真的打到了底层：calls=2
  [PASS] 流水线级键：查询+检索证据一致则同键，证据内容或顺序变则换键   证据顺序会改变上下文，所以必须换键

============================================================================================
汇总
============================================================================================
  10/10   A 文本相似性 · ROUGE（后端：rouge-lib）
  10/10   B 关键信息抽取与召回
  9/9   C 幻觉信号检测
  6/6   D 可读性
  14/14   E 缓存：键 / TTL / 容量 / 温度门限 / 落盘 / 线程安全
  9/9   F 批量处理：顺序 / 错误隔离 / 并发度
  8/8   G 缓存包装器与阶段七生成器的接口兼容（假生成器，不调模型）

总计 66/66 项通过   用时 12.3s
